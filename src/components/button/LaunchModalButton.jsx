import Button from 'react-bootstrap/Button';

const LaunchModalButton = ({a}) => {
    return (
        <Button variant="primary" onClick={a}>
            
        </Button>
    )
}

export default LaunchModalButton